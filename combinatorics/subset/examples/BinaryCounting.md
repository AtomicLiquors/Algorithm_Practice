“부분집합 생성을 위한 가장 자연스러운 방법”

사전적 순서로 부분집합 생성하기.

```java
  int arr[] = {3, 6, 7, 1, 5, 4};
		int n = arr.length;

		for(int i = 0; i < (1 << n); i++)
		{
			for(int j = 0; j < n; j++)
			{
				if( (i & (1<<j)) != 0)
					System.out.print(arr[j] + " ");
			}
			System.out.println();
		}
  ```

- 원소 수에 해당하는 N개의 비트열을 이용한다.
    - n번째 비트값이 1이다 = n번째 원소가 포함되었다
- ‘|’ 연산은 쓰지 않음. 비트를 새로 추가할 일은 없다.

[🗣️“특정 자리 비트 끄는 건 v & ~(1<<n) 로 할 수 있겠네요!”]
